import React from 'react';
import styled from 'styled-components';

const Header = () => {
  return <HeaderMain>header test</HeaderMain>;
};

export default Header;

const HeaderMain = styled.div`
  background-color: pink;
`;
